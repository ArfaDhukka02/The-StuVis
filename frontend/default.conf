resolver 127.0.0.11 valid=30s;
server {
    listen 80;

    # Serve static files
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ =404;
    }

    # Proxy anything under /api/ to the API container
    location /api/ {
        proxy_pass http://api:5000/;
    }
}
